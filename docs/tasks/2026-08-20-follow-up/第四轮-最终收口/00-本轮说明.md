# 第四轮：最终质量收口

状态：已完成。

合并顺序：B → C → D → E → A。

## 规则

- 不增加新功能、页面、公共字段和接口。
- 只回归、修复、补边界测试和更新有效项目文档。
- 不制作报告、展示材料、截图或部署产物。
- B、C、D、E 完成后由 A 执行最终全仓集成。

## 完成结果

- macOS `npm ci`、`npm run check` 和 `npm run dev` 通过。
- 四个前端不读取运行时业务测试夹具。
- 作业、调课、反馈、请假、排课和账号跨端流程通过。
- 未发现未解决的 P0 或 P1 问题。
- Windows PowerShell 最终全仓复核仍需一名 Windows 成员执行并回填。
- `develop` 在 A 最终提交推送后与远程同步。

本目录中的成员任务仅作为已完成范围和 PR 评审记录。
