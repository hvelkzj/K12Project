# B 成员第四轮任务

分支：`fix/B-parent-final-round-4`

## 任务

1. 回归登录、学生切换、课表、请假、通知和反馈。
2. 检查加载中、空数据、网络错误、401 和重试。
3. 检查快速切换学生和重复提交。
4. 检查窄屏布局和键盘操作。
5. 删除运行时 Mock、调试输出和假成功提示。
6. 只修复家长端问题，补齐最终边界测试。

## 验证

```text
npm run test --workspace @k12/parent-web
npm run typecheck --workspace @k12/parent-web
npm run build --workspace @k12/parent-web
npm run check
```
