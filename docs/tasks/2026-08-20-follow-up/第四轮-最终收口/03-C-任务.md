# C 成员第四轮任务

分支：`fix/C-student-final-round-4`

## 任务

1. 回归首页、课件、作业列表、详情、提交和结果。
2. 检查草稿、筛选、截止、附件、重复提交和订正历史。
3. 检查加载中、空数据、网络错误、401 和重试。
4. 保留 `AssignmentList.vue` 主视觉。
5. 删除运行时 Mock和调试输出。
6. 只修复学生端问题，补齐最终边界测试。

## 验证

```text
npm run test --workspace @k12/student-web
npm run typecheck --workspace @k12/student-web
npm run build --workspace @k12/student-web
npm run check
```
