# A 成员第四轮任务

分支：`fix/A-final-integration-round-4`

## 任务

1. 等 B、C、D、E 最终 PR 合并后同步最新 `develop`。
2. 在 macOS 运行 `npm ci` 和 `npm run check`。
3. 使用根目录统一启动 API 和四个前端。
4. 验证六角色登录、会话恢复和退出。
5. 验证作业、调课、反馈、请假、排课和账号联动。
6. 修复公共 API、共享类型、根脚本和集成问题。
7. 更新 README、项目计划和模块完成清单。
8. 删除有效文档中的旧截止时间和过期 Mock 说明；不删除历史 AI 记录。
9. 确认公共命令兼容 macOS 和 Windows。
10. A 的 PR 最后合并。

## 最终验证

```text
npm ci
npm run check
npm run dev
```

PR 中记录 macOS 结果、全部流程状态和仍需 Windows 复核的内容。
