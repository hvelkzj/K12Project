# D 成员第三轮任务

分支：`fix/D-teacher-cross-end-round-3`

## 任务

1. 使用真实 API 发布作业并读取学生提交。
2. 完成批改和要求订正。
3. 发送反馈并确认家长端可以读取。
4. 提交调课并确认后台可以审批。
5. 读取后台排课变化。
6. 读取已批准请假并正确记录 `LEAVE`。
7. 修复教师端数据回写、表单保留和错误提示问题。
8. 不修改 API、公共类型或其他前端。

## 必须验证

- 作业、反馈、调课、请假四条教师端流程。
- 教师与班主任权限边界。
- 取消课次禁止教学写操作。
- `401`、`403`、`409`、网络错误。
- 根目录 `npm run check`。
