# K12 项目后续任务归档

> 本目录保留第二、第三、第四轮的成员任务，用于追溯 PR 范围和评审标准。各轮已完成，不再作为待发布任务。

## 目录

1. `第二轮-剩余业务功能`：通知已读、请假审批、排课维护和账号启停。
2. `第三轮-跨端联调`：作业、调课、反馈、请假、排课和账号联动。
3. `第四轮-最终收口`：回归、边界修复、有效文档和跨平台验收。

## 执行结果

- 所有成员 PR 已审核、修复并合并到 `develop`。
- A 已执行最终 macOS 全仓集成检查。
- Windows 成员的最终复核结果在完成后记录到 A 第四轮交付文档。
- 后续不根据本目录自动生成新轮次或新截止时间。

## 保留的统一要求

- 公共字段、API 契约和共享类型由 A 统一维护。
- 公共脚本兼容 macOS 和 Windows，不使用本机绝对路径或单系统命令。
- 安装使用 `npm ci`；仅在修改依赖时运行 `npm install` 并提交根锁文件。
- 不安排报告、展示材料、部署、真实文件上传或 PostgreSQL 运行时接入。
