# K12 项目后续任务安排

> 本目录于 2026-08-21 从组长提供的任务文件归档到 `develop`，用于后续 PR 对照评审。原定日期和顺序作为历史基线保留；实际执行状态以 GitHub PR、`develop` 已合并内容和组长最新指令为准，不因归档而自动发布下一轮任务。

最终截止：2026 年 8 月 24 日 20:00。

## 执行前提

- 假设上一轮 A、B、C、D、E 的真实业务 API 接入任务均已完成、审核并合并到 `develop`。
- 本目录只包含后续第二、第三、第四轮任务，不重复上一轮内容。
- 每轮全部 PR 审核、修复并合并后，才能发布下一轮。

## 目录

1. `第二轮-剩余业务功能`：补齐通知已读、请假审批、排课维护、账号启停和相关页面。
2. `第三轮-跨端联调`：使用真实 API 验证作业、调课、反馈、请假、排课和账号联动。
3. `第四轮-最终收口`：停止新增功能，只做回归、修复和跨平台验收。

## 统一要求

- 所有分支从最新 `develop` 创建。
- 只修改本人模块、本人交付记录和本人 AI 记录。
- 公共字段、API 契约和共享类型由 A 统一维护。
- 不安排报告、演示、截图、部署、真实文件上传和 PostgreSQL 运行时接入。
- PR 必须说明修改范围、接口影响、测试结果和遗留问题。
- 提交前运行本人工作区测试、类型检查、构建及根目录 `npm run check`。
- 公共脚本必须兼容 macOS 和 Windows，不使用本机绝对路径或单系统命令。

## 合并安排

| 轮次 | PR 截止 | 合并顺序 |
|---|---|---|
| 第二轮 | 8 月 20 日 20:00 | A → D → B → E → C |
| 第三轮 | 8 月 22 日 18:00 | A → D → C → B → E |
| 第四轮 | 8 月 24 日 12:00 | B → C → D → E → A |

全部内容必须在 8 月 24 日 20:00 前合并到 `develop`。
