# B 家长端第三轮跨端修复交付记录

完成日期：2026-08-22

分支：`fix/B-parent-cross-end-round-3`

## 已完成

- 接入 `PATCH /parent/notifications/:notificationId/read`。
- 通知页支持普通通知和调课通知标记已读。
- 调课通知继续展示原日期时间、新日期时间、原教师和代课教师。
- 请假提交成功后刷新当前学生概览，用真实 API 同步审批状态。
- 请假页新增“刷新审批状态”，用于查看后台批准或拒绝后的最新状态。
- 反馈确认和异议仍以服务端返回结果更新页面。
- 异议失败、`409` 和网络错误不改变原反馈状态。
- 通知标记已读失败、`409` 和网络错误不改变原通知状态。
- 业务接口 `401` 继续清除 Token 并返回登录页。
- 概览加载增加请求序号保护，避免快速切换学生时旧请求覆盖新学生数据。

## 未修改范围

- 未修改 API。
- 未修改公共类型或状态枚举。
- 未修改其他成员前端。

## 测试覆盖

- 家长端业务客户端通知已读请求和返回状态。
- 通知已读 `409` 和网络错误。
- 普通通知已读状态替换。
- 调课通知嵌套 `notification.readAt` 替换。
- 请假重复提交、字段错误和网络错误。
- 反馈确认、异议、异议必填错误。
- `401` 清除 Token，其他业务错误保留登录状态。

## 待配合

- 成员 A/API 侧继续保持通知已读接口路径和响应体稳定。
- 请假批准和拒绝由后台完成，家长端通过刷新当前学生概览展示最新状态。
