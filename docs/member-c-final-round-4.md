# C 学生端第四轮交付记录：最终质量收口

完成日期：2026-08-23

分支：`fix/C-student-final-round-4`（基于最新 `develop`，含前三轮合并结果、A 的批改显示修复与 B 的第四轮合并）

## 回归结果

- 真实 API 回归 13/13 通过：教师发布作业 → 学生读取 → 首次提交 → 教师端可读取 → 要求订正 → 学生订正（attempt 2）→ 完整历史 → 教师批改（95 分）→ 学生查看结果；重复提交 `409`、空内容 `422`、无效 Token `401`、截止后提交 `409`。
- 学生端测试 66/66 通过；typecheck / build 通过；根目录 `npm run check` 通过。
- 首页、课件、作业列表、详情、提交、结果页面流程保持；`AssignmentList.vue` 主视觉未改动。

## 检查项

- 草稿：按作业 ID 隔离、提交成功清除、失败/返回/刷新保留、订正草稿不覆盖历史（单元测试覆盖）。
- 筛选：状态 × 课程组合、筛选无结果明确状态。
- 截止与附件：前端提前提示、服务端 `409`/`422` 为最终结果。
- 加载/空数据/网络错误/`401`/重试：页面状态齐全，`NETWORK_ERROR` 与 `HTTP_ERROR` 回退有测试覆盖。
- 运行时 Mock 与调试输出：学生端 `src` 无 `console.*`/`debugger`；`mockData.ts` 仅在测试文件中引用，页面运行时不再读取业务 Mock。

## 补齐的最终边界测试（7 项）

- 业务客户端：服务端 `500` 返回非 JSON 时使用稳定回退（`HTTP_ERROR`）、返回 JSON 时显示服务端消息，均不清除 Token。
- 提交校验：恰好 10 MB 的附件可以通过（边界值）。
- 提交历史：即使输入乱序也按 `attempt` 升序返回。
- 空概览：派生空作业列表、空课件列表，完成率为 0。
- 课件搜索：空课件列表搜索返回空数组。
- 草稿：载入的草稿是副本，修改副本不影响存储。

## 未修改

- 未新增功能、页面、公共字段或接口；未修改 API、公共类型或其他前端。

## PR 评审结果

- PR #30 已批准并合并到 `develop`。
- 评审发现部分 macOS/Windows 浏览器可能不给允许的附件扩展名提供 MIME，导致文件选择器允许但提交校验拒绝。
- 合并后已按 `.pdf/.docx/.jpg/.jpeg/.png` 补齐规范 MIME；未知扩展或明确的危险 MIME 仍会被拒绝。
- 作业页“日历”和“消息通知”原为可点击但无响应的占位入口；第四轮不新增功能，因此保留视觉并明确禁用为“暂未开放”。
- 浏览器回归确认首页、课件、作业列表、详情和空提交错误正常，控制台无错误或警告。
