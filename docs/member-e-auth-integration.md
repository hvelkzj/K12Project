# 成员 E 后台认证接入交付记录

日期：2026-08-11。集成复核：2026-08-12。

## 范围

- 修改 `apps/admin-web` 和成员 E 的记录。
- 后台业务数据继续使用 Mock。
- 认证接入 `POST /auth/login`、`GET /auth/me`、`POST /auth/logout`。
- 公共字段和角色从 `@k12/shared` 使用。

## 已完成

- 教务使用 `academic_901`，系统管理员使用 `system_999`，统一测试密码为 `K12Demo123!`。
- Token 保存在 `sessionStorage` 的 `k12AccessToken`；401 和退出后清除 Token。
- 用户字段统一为 `username` 和 `active`，不再使用 `account` 和 `enabled`。
- 教务仅能访问所属校区；系统管理员能访问单一机构下全部校区。
- 保留调课审批、拒绝原因必填、同校区代课选择和反馈工单关闭流程。
- 未登录或非管理员不能进入后台业务页面。
- 审批人和工单处理人使用认证接口返回的真实用户 ID。

## 测试覆盖

- 两种管理员登录和非管理员角色拒绝。
- 教务跨校区拒绝和系统管理员全校区访问。
- 拒绝原因必填、重复审批拒绝和代课教师范围。
- 工单关闭结果必填。
- Token 过期、退出和无 Token 退出。
- 未登录页面访问守卫和审计用户 ID。

## Windows 验证

- Node.js v24.16.0、npm 11.13.0。
- `npm ci`：通过。
- `npm run check`：通过。
- `npm run dev:api`：启动成功，`/health` 返回 200。
- 教务登录、当前用户、退出、退出后 401：通过。
- 系统管理员登录：通过。

## 集成复核

PR #5 已合并到 `develop`。A 在 PR 会话记录并修复了未登录页面入口、审计用户 ID 和重复管理员角色类型；相关测试与全仓检查通过。

## 需要配合

- A 继续维护公共角色、认证响应和字段契约。
- B、D 与 E 后续联调调课通知、审批、代课和反馈工单数据。
