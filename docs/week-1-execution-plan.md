# 第一周执行计划

时间：2026-07-27 至 2026-08-02。

本周目标：确定公共字段和接口，搭好五个端的页面骨架，完成最简单的登录和 Mock 流程。

## 统一约定

- 前端：Vue 3 + TypeScript。
- 后端：Node.js + TypeScript。
- 数据库：PostgreSQL。
- macOS 和 Windows 统一使用 Node.js 22.12+、npm 10+。
- 安装已有依赖使用 `npm ci`，两种系统使用相同的 npm 脚本。
- A 维护公共数据库和接口字段。
- B/C/D/E 发现字段不够时先告诉 A，不自行创建另一套同名字段。

## 每天做什么

| 日期 | 全组任务 | 完成标志 |
|---|---|---|
| 7/27 | A 搭项目；B/C/D/E 写本模块页面和字段清单 | 项目能启动，五份清单完成 |
| 7/28 | A 汇总数据库；各成员制作页面骨架 | 数据表和主要页面确定 |
| 7/29 | 全员检查接口字段；A 发布公共类型和 Mock | 各端使用同一套字段 |
| 7/30 | 每人完成一条本端简单操作 | 页面可点击并显示结果 |
| 7/31 | 调课和作业第一次跨端联调 | 前一步数据能在下一端看到 |
| 8/1 | 修复问题并补测试 | 每人至少完成 5 个测试场景 |
| 8/2 | 整理记录，不新增功能 | 第一周任务全部验收 |

## 7/27 A 成员交付

状态：已完成。

| 交付 | 位置或命令 | 结果 |
|---|---|---|
| 后端项目 | `apps/api` | 3000 端口，提供 `/health` |
| 家长端 | `apps/parent-web` | 5173 端口，交给 B |
| 学生端 | `apps/student-web` | 5174 端口，交给 C |
| 教师/班主任端 | `apps/teacher-web` | 5175 端口，交给 D |
| 教务/系统后台 | `apps/admin-web` | 5176 端口，交给 E |
| 全部启动 | `npm run dev` | 五个地址均返回 HTTP 200 |
| 提交前检查 | `npm run check` | 规范、类型、测试、构建通过 |

成员第一次使用：

1. 安装 Node.js 22.12 或更高版本。
2. 在项目根目录运行 `npm ci`。
3. 运行自己模块的 `npm run dev:<模块>` 命令。
4. 需要修改配置时，按 README 中对应系统的命令复制 `.env.example`。
5. 提交前运行 `npm run check`。

7/27 不提前定义业务字段。B/C/D/E 仍需在 7/28 前把字段清单交给 A。

## 7/28 A 成员交付

状态：已完成。

| 交付 | 位置 | 结果 |
|---|---|---|
| B/C/D/E 字段汇总 | `docs/a-field-summary-2026-07-28.md` | 15 个字段组、131 行字段提案已汇总 |
| 五方字段决议 | `docs/abcde-field-decisions-2026-08-07.md` | 9 个跨端问题已关闭 |
| 公共字段契约 | `docs/api-field-contract.md` | 名称、ID、时间、状态和权限范围已统一 |
| 公共数据模型 | `docs/data-model.md` | 23 张表及主要关联已确定 |
| PostgreSQL 初始迁移 | `apps/api/db/migrations/001_initial.sql` | 基础数据和四端业务表已建立 |
| 结构测试 | `apps/api/test/schema.test.ts` | 校验公共实体、状态和关键约束 |

7/28 不实现 B/C/D/E 的页面业务。7/29 全员按公共字段契约检查页面模型，A 再发布公共 TypeScript 类型、测试账号和 Mock 数据。

## 成员分支集成交付

状态：已合并到 `develop`。

| 成员 | 合并分支 | 已交付内容 | 集成结果 |
|---|---|---|---|
| B | `feature/B-parent` | 家长端首页、学生切换、课表、请假、通知、反馈，5 个 Mock 测试，两份 AI 记录 | 页面和测试已接入现有 Vue 工作区，并对齐公共字段 |
| C | `feature/C-assignment` | 作业字段清单和 AI 记录 | 原始提案作为历史交付保留；`feature/C-assignment-pages` 按要求不合并 |
| D | `feature/D-teacher` | 教师/班主任端 7 个页面、字段清单、两份 AI 记录 | 页面已合并，并对齐公共 ID、状态和字段 |
| E | `feature/e-admin-web` | 后台字段清单和 AI 记录 | 原始提案保留，最终结构指向公共契约；该分支没有页面代码 |

合并时保留根目录 `package-lock.json`、统一端口和现有 Vue/Vite 版本，没有引入成员分支中的重复锁文件。

页面整合和补齐结果见 `docs/module-page-inventory.md`。四个业务端共计划 29 个页面，当前完成 29 个；其中 B 补齐登录页，C 补齐 7 个学生端页面，D 原有 7 个页面完整，E 补齐 8 个后台页面。

## A：公共架构、数据库、登录权限

任务：

1. 创建后端和四个前端项目目录。
2. 配置项目启动、代码检查和环境变量示例。
3. 建立用户、角色、校区、班级、课程、排课等基础表。
4. 汇总 B/C/D/E 提交的字段。
5. 完成登录、当前用户和退出接口。
6. 准备家长、学生、教师、班主任、教务、管理员测试账号。
7. 提供公共类型和 Mock 数据。

依赖：

- B 提供家长、学生绑定和通知字段。
- C 提供作业和提交字段。
- D 提供签到、反馈、调课和批改字段。
- E 提供审批、代课和校区权限字段。

交给其他成员：

- 周一：项目目录和启动命令（已完成，见“7/27 A 成员交付”）。
- 周三：公共字段、测试账号和 Mock 数据。

## B：家长端和通知

任务：

1. 创建登录、首页、学生切换、课表、请假、通知和反馈页面。
2. 确定请假需要的字段。
3. 确定调课通知展示的原时间、新时间和代课教师。
4. 完成“查看课表 → 提交请假”的 Mock 操作。
5. 测试家长不能查看未绑定学生。

前置和交接：

- 周二前把绑定、请假和通知字段交给 A。
- 调课通知字段需要与 D、E 确认。
- 周三后使用 A 提供的公共类型和 Mock。

## C：学生端和作业

任务：

1. 创建登录、首页、课件、作业列表、作业详情、提交和结果页面。
2. 确定作业、附件、提交记录和批改结果字段。
3. 确定未提交、已提交、已批改、需订正状态。
4. 完成“查看作业 → 提交作业”的 Mock 操作。
5. 测试重复提交、截止后提交和附件错误。

前置和交接：

- 周二前把作业和提交字段交给 A。
- 作业字段和批改结果需要与 D 确认。
- 周三后使用 A 提供的公共类型和 Mock。

## D：教师/班主任端

任务：

1. 创建登录、今日课程、签到、作业发布、批改、反馈和调课页面。
2. 确定签到、作业、反馈和调课字段。
3. 区分教师和班主任可以查看的数据。
4. 完成“查看课程 → 提交调课”的 Mock 操作。
5. 测试教师访问其他班级和重复签到。

前置和交接：

- 周二前把调课、反馈和批改字段交给 A。
- 作业字段与 C 确认。
- 调课和反馈状态与 B、E 确认。
- 周三后使用 A 提供的公共类型和 Mock。

## E：教务和系统后台

任务：

1. 创建登录、工作台、排课、审批、代课、反馈工单、用户和看板页面。
2. 确定审批、代课和工单字段。
3. 确定教务和管理员的数据范围。
4. 完成“查看申请 → 通过或拒绝”的 Mock 操作。
5. 准备两个校区、三个班级和几条排课的测试数据。
6. 测试重复审批和跨校区访问。

前置和交接：

- 周二前把审批、代课、权限和测试数据交给 A。
- 审批页面依赖 D 的调课申请字段。
- 工单页面依赖 D 的反馈和 B 的家长异议。
- 周三后使用 A 提供的公共类型和 Mock。

## 两条联调顺序

调课：

`D 提交申请 → E 审批并安排代课 → B 查看通知 → A 检查权限`

作业：

`D 发布作业 → C 提交作业 → D 查看并批改 → C 查看结果`

## 第一周验收

- [ ] macOS 和 Windows 都能完成 `npm ci`。
- [ ] macOS 和 Windows 都能启动后端和四个前端。
- [ ] 六种角色可以使用测试账号登录。
- [ ] 各端使用相同的公共字段。
- [ ] 每人完成一条可点击的操作流程。
- [ ] 两条跨端流程可以使用 Mock 数据演示。
- [ ] 每人完成至少 5 个测试场景。
- [ ] 每人保存 AI 提示词、回复和自己的判断。

## 给 AI 的简单提示

```text
先阅读 AGENTS.md、docs/project-plan.md 和 docs/week-1-execution-plan.md。
我是成员【A/B/C/D/E】，只完成文档中我负责的任务。
本次任务是【具体任务】，只修改【负责目录】。
不要修改其他成员模块和公共字段；如果公共字段不够，先提出问题。
完成后运行测试，并说明修改了什么、测试结果和还需要谁配合。
代码和文档必须同时兼容 macOS 与 Windows，不使用单系统命令或本机绝对路径。
```
