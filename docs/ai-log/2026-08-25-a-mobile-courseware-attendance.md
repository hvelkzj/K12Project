# 2026-08-25 A 多端、课件与考勤完善 AI 记录

## 日期

2026-08-25

## 目标

在最终网页项目基础上，增加真实 Android APP 与微信小程序源码，统一学生概览契约，补齐教师课件发布、学生和家长考勤展示，执行跨端自动化与压力测试，并为 A 实验报告准备最终证据。

## 完整提示词

> PLEASE IMPLEMENT THIS PLAN:
>
> # A 成员多端完善与实验报告交付计划
>
> ## 1. 执行顺序
>
> 1. PR #34 完成 Windows 验证后合并至 `develop`。
> 2. 创建 `feature/A-student-mobile`，完成 Android APP 和微信小程序。
> 3. 两端验证通过并合并后，创建 `feature/A-courseware-attendance-completion`。
> 4. 补齐教师课件发布、学生及家长考勤展示。
> 5. 执行自动化测试、压力测试和跨平台验证。
> 6. 基于最终 `develop` 编写实验报告并生成提交包。
>
> ## 2. Android APP 与微信小程序
>
> 新增 `apps/student-mobile` 工作区，采用 uni-app、Vue 3 和 TypeScript，一套业务源码构建 Android APP 和微信小程序。将学生概览统一为公共 `StudentOverview`，复用 `@k12/shared`、认证、学生概览、提交、文件上传下载接口，不创建重复角色、状态或字段。
>
> 页面包含登录、首页、课件、作业、作业详情、提交和我的；支持中文业务界面、学生角色限制、会话恢复、课件下载、作业分类、历史提交、批改结果、考勤、拍照/图片附件和微信会话文件。附件支持 PDF、DOCX、JPG、PNG，单个文件不超过 10 MB；上传成功后再创建提交。
>
> Token 使用端侧本地存储，401 清理会话，403 显示无权进入，网络错误保留表单和草稿，提交期间防止重复操作。服务地址由构建配置注入，Android 模拟器默认使用 `10.0.2.2`，微信开发者工具使用本地开发配置。页面不出现面向开发者的技术词语。
>
> 工作区提供 `dev:app`、`build:app`、`dev:mp-weixin`、`build:mp-weixin`、`test` 和 `typecheck`，根目录增加对应入口并更新唯一锁文件。交付 APP 资源/测试 APK 和可导入微信开发者工具的小程序目录。
>
> 自动化测试覆盖登录、角色拒绝、会话、概览、课件、下载/打开失败、作业状态、首次/重复/截止/订正提交、附件校验、批改结果、考勤中文状态、401/403/409/422/网络错误和失败状态。端侧使用同一学生账号完成完整流程并保存截图。
>
> ## 3. 课件与考勤业务闭环
>
> 新增 `POST /teacher/courseware`，请求包含班级、课程、标题、说明和附件，返回 `Courseware`。教师身份取自会话，必须实际教授对应班级课程，附件属于当前教师，服务端生成 ID 和发布时间，重复发布返回 409，字段或业务错误返回 422。教师端增加发布课件，学生网页、APP 和小程序均可读取下载。
>
> 学生概览和家长学生概览返回考勤。学生展示本人出勤统计和历史，家长课表展示绑定学生考勤，状态显示“已出勤”“迟到”“请假”“缺勤”“未签到”，无记录显示“暂无考勤记录”，后端执行本人和绑定校验。
>
> ## 4. 整体测试与报告
>
> 补充教师课件到学生三端、移动提交到教师批改、批改回写移动端、考勤到学生家长以及跨学生/班级/校区拒绝测试。服务端测试使用独立仓库。
>
> 新增 Node.js 压力测试：六类账号登录并复用会话，对家长、学生、教师和后台概览发起约 500 次请求，最高并发 50，输出吞吐量、平均、P50、P95、最大耗时和失败数，验证服务稳定和权限，不设置依赖设备性能的固定耗时门槛。
>
> macOS 和 Windows 执行 `npm ci`、`npm run check`、`npm run test:load`、`npm run build:app` 和 `npm run build:mp-weixin`，同时完成 Android 安装和微信开发者工具验证。
>
> A 实验报告采用功能介绍、AI 交互、系统设计、测试调试、使用示例、团队合作、项目规则与完整交互记录七部分；增加多端架构、公共契约复用、APP/小程序截图、作业闭环案例及多环境测试结果。采用积极、客观的团队协作表达，准确区分个人贡献、公共协调和成员接口成果。
>
> ## 5. 最终提交
>
> 桌面生成 `黄章杰+黄章杰.zip` 和 `黄章杰+黄章杰_实验报告.pdf`。ZIP 根目录包含 `黄章杰+黄章杰_实验报告.docx` 和 `黄章杰+黄章杰_代码/`。代码目录包含最终网页端、APP、小程序、统一服务、公共包、测试、文档和锁文件，排除 `.git`、`node_modules`、缓存、覆盖率、临时文件、环境密钥和编辑器配置。交付前核对 APK、小程序目录、Word/PDF、可重新安装检查和所有实际统计。

## AI 回复摘要

- 同步 `develop` 并确认 PR #34 已合并，建立 A 多端开发分支。
- 调研 uni-app 官方构建方式，选择 Vue 3 CLI 依赖并建立真实移动工作区。
- 统一学生概览，完成移动认证、网络、文件、页面和测试。
- 扩展课件与考勤服务端联动，更新教师、学生和家长页面。
- 增加 500 请求压力测试和公共文档，执行根检查与双端构建。
- 在微信开发者工具游客模式导入真实构建目录，完成登录、课件、作业提交、个人中心和退出验证。
- 将 HBuilderX 工程导入并安装 App 真机运行插件；取得正式 AppID，使用云端证书和快速安心模式免费生成 Android APK。
- 根据可用工具区分源码构建、设备安装和最终制品证据，未把尚未执行的设备步骤写成已通过。

## 人工判断

- APP 和小程序共用业务源码、公共实体与状态，降低三端数据含义不一致的风险。
- 移动文件使用 base64 适配 uni-app 端侧读取能力；服务端解码后按原始文件执行 10 MB 限制。
- `StudentOverview` 放入公共包，网页端和移动端不再维护重复响应类型。
- 课件发布校验实际授课关系和附件所有者，同名重复返回冲突，避免前端隐藏入口代替权限。
- 压力测试使用真实 HTTP 与独立内存仓库；耗时只记录本机结果，不设置跨设备硬门槛。
- 设备安装证据只在 HBuilderX、Android 设备和微信开发者工具真实完成后写入报告。
- 首次小程序登录发现预填密码与公共测试账号不一致；统一修正为 `K12Demo123!`，补测试并重新构建后登录成功。
- 小程序游客模式的系统能力会产生工具自身警告；项目问题面板为 0，业务验证以页面状态和真实本地服务数据回写为准。

## 执行的修改

- 新增 `apps/student-mobile` 及 6 个页面、客户端、文件能力、会话状态、测试和端侧文档。
- 新增公共 `StudentOverview`，扩展家长和教师概览。
- 新增教师课件发布接口、仓库规则、教师页面和客户端测试。
- 新增学生/家长考勤展示和相关测试夹具。
- 新增移动 base64 文件传输与服务端校验测试。
- 新增跨平台移动构建脚本和压力测试脚本。
- 更新契约、业务规则、项目计划、页面清单、README、交付记录和报告证据。

## 测试结果

- `npm run check`：通过；321 项有效测试通过，0 失败，API 2 项受限端口用例跳过。
- `npm run build:app`：通过，生成 APP 运行资源。
- `npm run build:mp-weixin`：通过，生成微信小程序构建目录。
- `npm run test:load`：500 请求、并发 50、失败 0；吞吐量 5760.16 请求/秒，平均 8.35 ms，P50 3.96 ms，P95 48.33 ms，最大 81.59 ms。
- 微信开发者工具：真实导入和业务流程通过。学生作业提交成功后，首页待完成数量由 2 变为 1；退出后返回学生登录页。
- HBuilderX：移动工程识别、App 真机运行插件和免费云打包通过；APK 约 14 MB，ZIP 结构与 RSA/SHA-256 签名验证通过。

## 下一轮问题

- 在 Windows PowerShell 完成本轮依赖、检查、压力和双端构建复验。
- 连接 Android 设备安装测试 APK，补录原生登录、课件、作业提交、状态回写和退出证据。
- Windows PowerShell 复验最终移动端命令。

## 真机登录问题跟进

### 完整提示词

> 经过实测，app端在输入账号密码后会卡在正在登录...
>
> 刚刚显示了网络连接失败，请稍后重试

### AI 回复摘要与人工判断

- 检查登录页、移动客户端、服务启动地址和真机访问条件。
- 确认 `10.0.2.2` 是 Android 模拟器访问宿主机的专用地址，不能作为真机访问电脑的地址。
- 确认旧请求没有显式超时，因此网络不可达时页面会长时间保持登录状态。
- 选择在登录页增加可保存的局域网连接设置，由服务启动日志跨平台输出手机可访问地址；不把某台电脑的固定 IP 写入源码。

### 执行的修改

- 登录页增加连接地址输入、健康检查和“正在连接/正在登录”分阶段提示。
- 切换服务地址时清理旧会话，连接地址保存在端侧本地存储。
- 请求和下载增加 10 秒超时，失败时提示检查同一 Wi-Fi、服务启动和地址。
- API 默认监听局域网，并使用 Node.js `networkInterfaces` 输出手机访问地址。
- Android 版本升级为 `0.1.1`，补充真机连接说明和回归测试。

### 测试结果

- 移动端 25 项测试通过，新增地址规范化、地址持久化/会话清理和请求超时测试。
- `npm run check` 通过：324 项有效测试通过，0 失败；类型检查及网页、APP、小程序构建通过。
- 临时启动 API 验证能够输出本机回环地址和局域网“手机访问地址”。
- HBuilderX 使用正式 AppID、云端证书和快速安心模式免费生成 `0.1.1` APK；压缩结构与签名验证通过，SHA-256 为 `e79eb8660b4ece5c40cc2655c9265b08222de3d81bfb1a36d44e8c01518dfe10`。

### 下一轮问题

- 安装 `0.1.1` APK，在真机填写服务日志输出的局域网地址，复验登录和完整业务流程。
