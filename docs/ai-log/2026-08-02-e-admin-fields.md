# 2026-08-02 E 成员：第一周第一天任务（教务/系统后台页面与字段清单）

## 目标

完成 E 成员第一周 7/27 的任务：整理本模块页面清单和字段清单，交付给 A 汇总数据库和公共字段。

## 完整提示词

> 先阅读 AGENTS.md、docs/project-plan.md 和 docs/week-1-execution-plan.md。
> 我是成员E，只完成文档中我负责的任务。
> 本次任务是完成E的第一天执行计划。
> 不要修改其他成员模块和公共字段；如果公共字段不够，先提出问题。
> 完成后运行测试，并说明修改了什么、测试结果和还需要谁配合。

## AI 回复摘要

- 创建 `docs/E-admin-fields.md`，包含页面清单、审批/代课/工单字段、权限范围与测试数据。
- 字段命名与 C 成员的 `docs/C-assignment-fields.md` 风格保持一致。

## 人工判断

待 E 成员审查后填写：

- [ ] 页面清单符合本端职责（登录、工作台、排课、审批、代课、工单、用户、看板）。
- [ ] 字段与 D 的调课申请、反馈字段以及 B 的家长异议字段是否对齐。

## 执行的修改

- 新增 `docs/E-admin-fields.md`。
- 未修改任何公共字段和其他成员模块。

## 测试结果

- 仅文档变更，不涉及代码；运行 `npm run check` 验证项目整体仍通过。

## 下一轮问题

1. D 的调课申请字段和反馈字段是什么？需要对齐审批与工单的关联字段。
2. B 的家长异议字段是什么？需要对齐工单的 `parent_id` 和 `issue`。
3. A 何时发布公共类型和 Mock 数据？后续页面按公共字段实现。
