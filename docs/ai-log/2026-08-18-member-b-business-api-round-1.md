# AI 协作记录：B 家长端业务 API 接入

日期：2026-08-18

## 目标

按 `docs/tasks/2026-08-17-round-1/02-B.md` 完成家长端业务 API 接入，只修改 `apps/parent-web` 和 B 自己的记录。

## 完整提示词

```text
已拉取最新 develop，根据最新任务完善成员 B 部分。
```

## AI 回复摘要

- 读取本轮 B 任务，确认只处理 `apps/parent-web` 和 B 本人记录。
- 对照 A 已合并的业务 API 契约和 API 测试，确认家长端 4 个接口的响应形态。
- 新增可注入 `fetch` 的 `parentBusinessClient`。
- 改造家长端页面运行时数据流，移除运行时业务 Mock。
- 增加加载、空数据、失败重试、401 清 Token 和业务错误展示。
- 使用测试夹具覆盖业务 API 客户端行为，不依赖后端运行。
- 补充交付记录。

## 人工判断

- 家长业务接口复用现有认证 Token，没有新增第二套登录状态。
- 页面运行时业务数据来自 API，Mock 只保留为测试夹具。
- `401` 清理 Token；其他业务错误保留登录状态并显示服务端信息。
- 未新增公共字段，未修改 API 和其他成员前端。

## 执行的修改

- 新增 `parentBusinessClient.ts` 和对应测试、夹具。
- 重构 `App.ts`，接入绑定学生、概览、请假和反馈接口。
- 删除 `mockData.ts`、`parentService.ts` 及旧 Mock 服务测试。
- 更新家长端加载、空数据、失败重试和按钮禁用样式。
- 新增 B 本轮交付记录。

## 测试结果

- 父端测试通过：22/22。
- 父端 TypeScript 类型检查通过。
- 父端构建通过。
- 根目录 `npm run check` 通过，PR 合并前全仓 114/114 项测试通过。

## 下一轮问题

- A 继续保持家长端 4 个接口和 `{ code, message }` 错误结构稳定。
- B 后续与 E 联调家长反馈异议生成的后台工单内容。
