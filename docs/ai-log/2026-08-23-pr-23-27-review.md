# 2026-08-23 PR #23–#27 评审、合并与修复

## 日期

2026-08-23。

## 目标

按现有任务和合并顺序处理 PR #23–#27，在 PR 评审中记录问题，合并后在 `develop` 修复并验证。

## 完整提示词

```text
处理pr#23-pr#27
```

## AI 回复摘要

- 读取第三轮统一说明和 A、B、C、D、E 任务。
- 核对五个 PR 的成员、轮次、目标分支、提交、文件和已有评审。
- 分别运行家长端、学生端、教师端和后台的测试、类型检查、构建及 ESLint。
- 在 PR #23、#24、#25、#27 提交问题记录和批准评审。
- 在 PR #26 说明关闭原因和与 PR #27 的冲突，不恢复旧快照。
- 按 #27 → #25 → #24 → #23 的依赖顺序合并。
- 在 `develop` 修复家长通知去重、学生批改显示、教师反馈草稿和后台无效统计测试。

## 人工判断

- PR #27 是 B 第二轮补交，必须先于第三轮处理。
- PR #26 与 #27 存在三个文件的真实合并冲突，且会回退未读数量、请假中文状态和重复点击保护，因此不能合并。
- API 同时返回通知摘要和调课通知详情时，前端应按通知 ID 去重并保留详情对象。
- 学生结果页应让同 attempt 的 overview 数据覆盖提交时快照，因为 overview 包含后续批改状态。
- 教师切换课次时，反馈正文和作业、调课草稿具有相同的数据隔离要求。
- 后台未接入页面的重复统计函数不能作为页面统计的测试证据。

## 执行的修改

- 家长端：通知去重、重复操作和旧请求保护 helper，并补 `403`、`404`、`409`、网络错误测试。
- 学生端：按 attempt 选择展示记录，显示单次提交得分，并补批改覆盖测试。
- 教师端：统一清空作业、反馈和调课草稿，并补回归测试。
- 后台：删除未接入页面的 `countTodaySchedules` 和无效测试。
- 文档：修正 C AI 记录中的 Windows 本机路径，补 B、D 第三轮交付记录，并更新 B、C、E 评审修复说明。

## 测试结果

- 家长端：35/35 通过。
- 学生端：59/59 通过。
- 教师端：43/43 通过。
- 后台：48/48 通过。
- API：44/44 通过。
- 共享包：7/7 通过。
- 根目录 `npm run check`：通过，共 236/236 项测试通过。
- 根目录 `npm run dev` 启动通过；API 和四个前端入口均返回 `200`。

## 下一轮问题

- Windows 成员需要基于最终 `develop` 运行 `npm.cmd ci`、`npm.cmd run check` 和四端及 API 启动命令。
- PR #26 保持关闭；B 后续工作必须从最终 `develop` 新建分支。
