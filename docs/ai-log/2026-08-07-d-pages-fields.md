# 2026-08-07 D 成员：教师端页面与字段清单

## 目标

只在 `apps/` 和 `docs/` 内完成 D 成员负责的教师/班主任端页面骨架与字段清单，不修改其他成员模块或公共字段。

## 完整提示词

> 先阅读 AGENTS.md、docs/project-plan.md 和 docs/week-1-execution-plan.md。
> 我是成员D，只完成文档中我负责的任务。
> 本次任务是写出D成员负责的模块页面和字段清单，只修改./apps和./docs。
> 不要修改其他成员模块和公共字段；如果公共字段不够，先提出问题。
> 完成后运行测试，并说明修改了什么、测试结果和还需要谁配合。
> 代码和文档必须同时兼容 macOS 与 Windows，不使用单系统命令或本机绝对路径。

## AI 回复摘要

- 完成登录、今日课程、课堂签到、发布作业、作业批改、课后反馈和调课申请页面骨架。
- 使用教师端本地 Mock 数据演示切换、填写与提交，不新增公共类型或 API 字段。
- 整理签到、作业、批改、反馈和调课字段提案。
- 标明教师与班主任的数据范围，以及需要 A、B、C、E 确认的接口内容。

## 人工判断

待 D 成员审查后填写：

- [ ] 页面范围符合 D 成员分工。
- [ ] 字段提案没有覆盖其他成员维护的公共字段。
- [ ] 教师和班主任的数据范围符合小组约定。
- [ ] Mock 文案和演示数据可用于课程作业展示。

## 执行的修改

- 更新 `apps/teacher-web/src/App.vue`，增加七个教师端页面骨架和本地 Mock 交互。
- 更新 `apps/teacher-web/src/style.css`，增加桌面与移动端响应式布局。
- 更新 `apps/teacher-web/index.html` 的页面标题。
- 新增 `docs/member-d-pages-and-fields.md`，记录 D 模块页面、字段提案、权限边界和协作项。
- 未修改 `apps/api`、其他前端、数据库、共享类型、角色枚举或状态枚举。

## 测试结果

- 首次运行 `npm run check`：未安装依赖，停止于 `eslint: command not found`。
- 运行 `npm ci`：按现有锁文件安装依赖，未修改依赖声明。
- 再次运行 `npm run check`：通过。
- ESLint：通过。
- 所有工作区类型检查：通过。
- API 既有测试：2 项通过，0 项失败。
- API 与四个前端生产构建：通过。
- 本地浏览器渲染：通过，控制台无错误。
- 页面导航：登录、签到、发布、批改、反馈 5 个场景通过。
- Mock 交互：调课提交提示和班主任数据范围切换通过。

## 下一轮问题

1. A 能否确认公共 ID 类型、字段命名、日期时间格式、角色/状态枚举和 Mock 数据？
2. B 能否确认调课通知展示字段、反馈确认和异议状态？
3. C 能否确认作业、附件、提交、批改结果和订正状态字段？
4. E 能否确认调课审批、代课、反馈工单字段和状态？
5. D 成员审查后，需要补充教师访问其他班级和重复签到的业务边界测试。
