# 2026-08-18 PR #14 评审与修复

## 日期

2026-08-18。

## 目标

根据 D 成员任务审核 PR #14，在 PR 中提出问题并同意合并，合并后在 `develop` 修复问题。

## 完整提示词

```text
根据文件中D成员的任务，处理PR#14，给出评审，提出问题，并且同意合并，合并后修复问题
```

## AI 回复摘要

- 读取 D 任务、项目协作规范和 PR #14 全部改动。
- 在独立临时工作区运行 `npm ci` 和 `npm run check`。
- 在 PR 中提交 3 条行内问题和批准评审。
- 合并 PR #14 后在 `develop` 修复认证、角色、范围、类型、测试和文档。
- 使用真实 API 完成教师和班主任浏览器回归。

## 人工判断

- PR #14 只有一个提交，新增模块未接入现有应用，不能通过类型检查。
- 问题不需要修改 API 或公共类型，可以在教师端独立修复。
- 按组长指定流程先记录问题、批准并合并，再在 `develop` 修复。
- 本次以用户指定的 D 任务文件为验收范围，业务 Mock 可以保留，认证必须真实。

## 评审问题

1. 登录路径错误，认证和 Token 工具为空文件。
2. 路由依赖、别名、布局和页面不存在，且没有在 `main.ts` 注册。
3. axios 错误加入根包，锁文件包含大量无关变化。
4. 角色、会话、数据范围、边界测试和交付记录未完成。

## 执行的修改

- 新增可注入 `fetch` 的教师认证客户端。
- 实现真实登录、当前用户、退出、会话恢复和过期清理。
- 限制教师与班主任角色，拒绝并撤销其他四种角色会话。
- 删除不可运行的路由、空工具、独立登录页和根 axios。
- 将真实认证直接接入现有教师端页面。
- 使用公共类型、大写状态、数字 ID 和带时区时间。
- 实现任课教师与班主任数据范围、本人授课写权限。
- 补充重复签到、重复调课和时间边界。
- 新增教师端测试脚本、21 项测试和 D 交付记录。

## 测试结果

- PR 合并前：`npm run check` 失败，教师端类型检查报告 16 个错误。
- 修复后教师端测试：21/21 通过。
- 修复后教师端类型检查、构建和 ESLint：通过。
- 修复后根目录 `npm run check`：通过；全仓 137 项测试中 136 项通过、1 项环境限制测试跳过。
- 浏览器真实 API 回归：教师登录、调课、重复拦截、退出通过。
- 浏览器真实 API 回归：班主任范围、只读限制、刷新恢复会话通过。
- 浏览器控制台：0 个错误、0 个警告。

## 下一轮问题

- Windows 成员运行 `npm.cmd ci`、`npm.cmd run check` 和对应启动命令。
- A 保持认证接口与错误结构稳定。
- 本轮不提前实现未在用户指定 D 任务中的下一轮业务 API 接入。
