# 2026-08-10 A 成员：认证交付分支与 PR

## 日期

2026-08-10。对应第一周 7/29 公共类型、测试账号和认证 API 交付。

## 目标

核对 A 的交付是否误入远程 `develop`，需要时回退；把 A 的公共类型、认证功能、测试和文档整理到 `feature/A-auth`，推送后重新提交以 `develop` 为目标的 PR。

## 完整提示词

> A：把昨晚未提交的内容整理到 feature/A-auth。分成公共类型、认证功能、测试/文档等可辨识提交。发起合并到 develop 的 PR，并说明公共类型、角色、状态和锁文件影响。Windows 验证通过后合并。后续公共字段不足时，由 A 统一裁决，其他成员不要自行新增同名公共字段。

> 我已经把内容推送到develop了，请你按照注释内容，完成A的工作，该退回develop退回，该发布feature/A-auth发布，重新提交pr

## AI 回复摘要

- 获取远程引用后确认 `origin/develop` 仍停在 `c677ced`，A 的内容只存在于本地未提交工作区，因此没有制造无效的 revert。
- 从远程 `develop` 基线创建并推送 `feature/A-auth`。
- 把交付拆成公共包、认证功能、边界测试和交付文档四个可辨识提交。
- 通过 GitHub 应用创建草稿 PR #4，目标分支为 `develop`。
- PR 描述列出公共类型、角色、状态、根锁文件和 API 依赖影响，并明确没有修改 B、C、D、E 页面。
- PR 保持草稿，等待 Windows 验证和组长公共契约审核。

## 人工判断

待 A 和组长审查后填写：

- [ ] 同意没有对 `develop` 创建无效回退提交。
- [ ] 同意四个提交的范围和顺序。
- [ ] 同意 PR #4 的公共字段、角色、状态和锁文件影响说明。
- [ ] Windows 验证通过后再把 PR 标记为可审查并合并。

## 执行的修改

- 创建并推送 `feature/A-auth`。
- 提交 `feat(shared): publish public contracts and mock accounts`。
- 提交 `feat(auth): add mock session endpoints`。
- 提交 `test(auth): cover session and request boundaries`。
- 提交 `docs(plan): record A week-one auth delivery`。
- 更新交付文档状态为“功能完成，待 Windows 验证和 PR 合并”。
- 创建草稿 PR：`https://github.com/hvelkzj/K12Project/pull/4`。
- 未修改 B、C、D、E 的业务页面。

## 测试结果

- macOS `npm ci`：通过。
- `npm run check`：通过；共发现 48 项测试，47 项通过、0 失败，1 项真实端口测试因 Codex 沙箱禁止监听而跳过。
- API 开发服务实际启动成功。
- 登录、当前用户、退出、退出后失效主流程返回 `200 / 200 / 204 / 401`。
- `git diff --check`：通过。

## 下一轮问题

1. Windows 成员需要运行 `npm ci`、`npm run check` 和 `npm run dev:api`。
2. 组长需要审核公共 API 契约、角色枚举和状态枚举。
3. 两项检查完成后，把 PR #4 标记为可审查并合并到 `develop`。
4. B、C、D、E 后续只在本人模块中接入 `@k12/shared` 和认证 API；公共字段不足时先交给 A 裁决。
