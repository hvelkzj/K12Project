# 2026-08-23 PR #30 评审与修复

## 日期

2026-08-23。

## 目标

审核 C 成员第四轮 PR #30；记录问题并批准合并，再在 `develop` 修复评审问题。

## 完整提示词

> 处理pr#30

## AI 回复摘要

- 核对 PR 目标、第四轮 C 任务、最新 `develop` 基线和文件范围。
- 审查新增边界测试并运行学生端、全仓和浏览器回归。
- 在批准评审中记录附件 MIME 跨平台兼容和无响应占位按钮问题。
- 合并后在 `develop` 归一化附件 MIME，并明确禁用未开放入口。

## 人工判断

- PR 新增测试有效，没有越界修改，可以合并。
- 已允许扩展名不能仅因浏览器未提供 MIME 而被拒绝。
- 未实现的功能不应保留为可点击且无响应的按钮。
- 第四轮不新增日历、消息页面或接口。

## 执行的修改

- `apps/student-web/src/studentService.ts`：增加允许附件扩展名到规范 MIME 的归一化。
- `apps/student-web/src/studentService.test.ts`：覆盖空 MIME、通用 MIME、别名、未知扩展和危险 MIME。
- `apps/student-web/src/views/Home.vue`：文件选择时使用统一 MIME 归一化。
- `apps/student-web/src/views/AssignmentList.vue`：日历和消息通知入口改为明确禁用。
- `docs/member-c-final-round-4.md`：记录 PR 评审、合并和修复结果。

## 测试结果

- PR 分支：学生端 66 项测试通过，类型检查、构建和定向 ESLint 通过。
- PR 分支：根目录 `npm run check` 通过。
- 浏览器：首页、课件、作业列表、详情、提交校验正常；控制台 0 错误、0 警告。
- 合并后：`npm ci` 通过。
- 合并后：学生端 67 项测试通过，类型检查、构建和定向 ESLint 通过。
- 合并后：根目录 `npm run check` 通过；全仓 255 项通过、1 项因执行环境禁止监听回环端口而跳过、0 项失败。
- 合并后浏览器：日历和消息通知入口均明确禁用，控制台 0 错误、0 警告。
- macOS 启动验证：API `/health` 和家长端、学生端、教师端、后台均返回 HTTP 200。

## 下一轮问题

- 第四轮全部成员完成后，由 A 做最终全仓集成检查。
