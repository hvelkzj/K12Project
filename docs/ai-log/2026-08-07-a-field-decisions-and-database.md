# 2026-08-07 A 成员：字段决议与 7/28 数据库汇总

## 日期

2026-08-07。对应第一周 7/28 数据库汇总任务。

## 目标

以 A、B、C、D、E 各自职责完成字段联合评审，自主解决上一轮所有待处理问题，并把统一结果落到公共字段契约、PostgreSQL 初始迁移和相关文档中。最终产物保留在 `develop` 分支。

## 完整提示词

> 请解决文档中的这些问题，需要统一对齐的地方由你自己来定夺，目的是为了后续的顺利开发，包括什么成员之间的讨论确定都由你来完成。处理完成后请对每一个问题做出回答，不要完全以A的口吻，要假设你分别是ABCDE去交流处理这些问题。
> 接下去是A的具体任务：解决待处理的问题后，相当于才完成了第一周计划的7-27的任务，请继续完成7-28日的A同学的任务。并继续完善相关文档
> 最后汇总的产物要在develop分支下

## AI 回复摘要

- 按 A、B、C、D、E 的职责模拟联合字段评审，并逐项关闭 9 个问题。
- 统一命名、数字 ID、时间格式、附件、作业、订正、调课、反馈和工单状态。
- 新增 PostgreSQL 初始迁移和结构测试。
- 发布公共字段契约与数据模型文档。
- 更新字段汇总、业务规则、第一周执行计划和 README。
- 未修改 B、C、D、E 的页面模块。

## 人工判断

待成员审查后填写：

- [ ] 同意五方联合决议作为 7/28 公共开发基线。
- [ ] 同意调课审批与代课第一版使用一条流程记录。
- [ ] 同意反馈状态与反馈工单状态分开维护。
- [ ] 同意 7/29 再发布 TypeScript 公共类型、登录接口和测试账号 Mock。

## 执行的修改

- 新增 `docs/abcde-field-decisions-2026-08-07.md`。
- 新增 `docs/api-field-contract.md` 和 `docs/data-model.md`。
- 更新 `docs/a-field-summary-2026-07-28.md`，把待确认问题改为最终回答。
- 新增 `apps/api/db/migrations/001_initial.sql`。
- 新增 `apps/api/test/schema.test.ts`。
- 更新 `README.md`、`docs/business-rules.md` 和 `docs/week-1-execution-plan.md`。
- 更新上一轮 AI 记录，指向本轮最终决议。
- 未修改其他成员页面或另建同义公共字段。

## 测试结果

- `npm run test --workspace @k12/api`：7 个测试通过，0 个失败；其中 5 个为迁移结构测试。
- `npm run check`：通过。
- ESLint：通过。
- 后端和四个前端类型检查：通过。
- 后端测试：7 个通过，0 个失败。
- 后端和四个前端生产构建：通过。
- `psql --version`：本机提供 PostgreSQL 17.10 客户端。
- `pg_isready`：本机 5432 端口没有运行 PostgreSQL 服务，因此未擅自创建数据库；本轮完成 SQL 结构和约束的自动化静态验证，实际迁移执行留给有本地数据库的成员复核。

## 下一轮问题

1. 7/29 是否发布公共 TypeScript 类型和前端可直接导入的状态常量？
2. 7/29 是否增加登录、当前用户、退出接口及六种测试账号？
3. B/C/D/E 是否已按 `docs/api-field-contract.md` 检查各自页面 Mock？
