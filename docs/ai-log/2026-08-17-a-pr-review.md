# 2026-08-17 A 成员：PR #8 学生端认证与作业提交复核

## 日期

2026-08-17。

## 目标

处理当前请求合并到 `develop` 的 PR。先复核变更、任务范围、测试和页面，再决定是否合并；发现问题时在 PR 中留下可执行的修改意见。

## 完整提示词

> 处理项目的PR请求

## AI 回复摘要

- 查询到当前仅有 PR #8 `feat(student): complete auth and assignment submission` 待处理。
- 对照 C 成员任务单复核文件范围、公共类型、认证、作业提交、页面清单和测试要求。
- 在 PR #8 的最新提交 `95be44f` 上执行依赖安装、全仓检查和浏览器验证。
- 发现测试数回退、首页与课件入口丢失、错误密码提示错误和 Windows 验收缺失等阻塞问题。
- 已在 PR #8 提交 `REQUEST_CHANGES` 评审，暂不合并到 `develop`。

## 人工判断

- [x] PR 只修改 C 学生端和 C 自己的文档，未越界修改其他成员模块或公共字段。
- [x] 作业截止、附件、订正历史和提交结果回写逻辑整体可用。
- [x] 阻塞问题会造成既有功能和测试倒退，不能直接合并。
- [x] 未在 `develop` 提前复制或修复尚未合并的 C 分支代码。

## 执行的修改

- 在 PR #8 正式提出以下问题：
  - 学生端由原有 13 项测试回退为 12 项，真实认证客户端没有单元测试。
  - 新页面壳层移除了既有首页和课件入口，课件数据无法从页面访问。
  - 错误密码的 401 被错误显示为“认证已过期，请重新登录”。
  - 会话恢复和退出登录需要区分 401、网络错误和 5xx。
  - 登录表单的 label 未与输入框关联，且需要阻止重复登录请求。
  - 缺少 Windows PowerShell 下的依赖、全仓检查、启动和浏览器流程验证记录。
- 本次只新增该 AI 交互记录；没有合并 PR #8，没有修改业务代码。

## 测试结果

- macOS `npm ci`：通过。
- `npm run check`：通过。
- 全仓发现 71 项测试：70 项通过、0 失败、1 项因当前环境无法监听临时端口而跳过。
- 学生端：12/12 通过，但不满足原有 13 项不得回退的任务要求。
- 浏览器正确登录可进入作业列表；页面控制台没有错误或警告。
- 浏览器错误密码验证稳定复现错误提示“认证已过期，请重新登录”。
- 浏览器页面语义检查确认账号和密码 label 均未关联输入框。

## 下一轮问题

1. C 修复 PR #8 的阻塞项并补充测试，学生端测试总数应大于 13。
2. C 恢复首页、课件入口与课件附件展示，同时保留现有作业清单视觉和完整提交流程。
3. Windows 成员完成 `npm ci`、`npm run check`、API/学生端启动及浏览器主流程验证。
4. C 推送修改并通知 A 复审；复审通过后再合并到 `develop`。
