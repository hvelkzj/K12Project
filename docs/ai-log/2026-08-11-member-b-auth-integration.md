# AI 协作记录：成员 B 家长端认证接入

日期：2026-08-11

## 输入

- 最新总任务：成员 B 负责 `apps/parent-web`。
- 最新 B 任务：接入真实 auth API，业务数据保留 Mock，使用共享类型和共享测试账号。

## AI 辅助内容

- 梳理 `@k12/shared` 已提供的用户、学生、课表、请假、通知、反馈和认证类型。
- 新增父端认证客户端，封装登录、刷新恢复、退出和 token 存储。
- 将父端 Mock 数据迁移到共享类型，停止使用本地重复公共类型。
- 补充认证和家长业务规则测试。
- 执行父端测试、类型检查和构建。

## 人工确认点

- 公共字段当前够用，未修改 `packages/shared`。
- 父端新增 `@k12/shared` 依赖声明用于稳定解析 workspace 包。
- 后续 PR 目标分支应为 `develop`。
