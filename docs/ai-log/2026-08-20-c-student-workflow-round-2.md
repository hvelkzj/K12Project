# AI 协作记录：C 学生端第二轮（筛选、搜索与本地草稿）

日期：2026-08-20

## 目标

按第二轮任务 `03-C-任务(1).md` 完成学生端：作业状态/课程筛选、课件标题搜索、按作业 ID 隔离的本地草稿、课件附件仅展示元数据、空数据状态；保留 `AssignmentList.vue` 主视觉。只修改 `apps/student-web` 和 C 交付/AI 记录。

## 完整提示词

```text
"D:\Wechat\...\03-C-任务(1).md" "D:\Wechat\...\00-本轮说明.md" 这是接下来的任务，去完成吧
```

## AI 回复摘要

- 读取第二轮任务与本轮说明，确认 C 的任务只涉及学生端前端功能，不涉及本轮统一接口（家长通知、请假审批、排课维护、账号启停均为其他成员）。
- 确认第二轮分支基于第一轮分支 `feature/C-student-api-round-1`（PR #17 尚未合并），保证提交流程与草稿功能建立在已完成的业务 API 之上。
- 新增四个纯逻辑模块并配套测试：组合筛选、课件搜索、草稿存储、提交流程。
- 页面接入：筛选栏、草稿预填/保存/清理、课件搜索、附件元数据展示。
- 移除虚假下载提示；补全空作业、空课件、筛选无结果状态。
- 全仓 `npm run check` 通过后整理交付记录。

## 人工判断

- 草稿只存正文与 `FileSummary` 元数据，不涉及真实文件上传；提交历史始终来自服务端，草稿不会覆盖历史。
- 本地存储键名 `k12StudentAssignmentDrafts` 与现有 `k12AccessToken` 风格一致；草稿存储可注入，测试不依赖浏览器。
- 筛选与搜索均为纯函数，放在独立模块便于测试；页面只做状态绑定。
- 未新增依赖、未修改锁文件、未修改 API 或公共类型；课件搜索和筛选无结果均有明确状态。
- 合并顺序为 A → D → B → E → C，本分支基于第一轮分支，待 PR #17 合并后本 PR diff 保持干净。

## 执行的修改

- 新增 `assignmentFilters.ts`、`coursewareSearch.ts`、`assignmentDrafts.ts`、`draftSubmission.ts` 及对应测试。
- 修改 `views/AssignmentList.vue`（筛选栏与空状态）、`views/Home.vue`（草稿预填/保存/清理）、`App.vue`（课件搜索与附件元数据）。
- 新增 C 第二轮交付记录和本 AI 记录。

## 测试结果

- 学生端测试：54/54 通过（含新增 19 项）。
- 学生端 typecheck / build：通过。
- 根目录 `npm run check`：通过（全部 workspace 的 lint、typecheck、test、build）。

## 下一轮问题

- 等待 PR #17（第一轮）与第二轮 PR 的评审；若评审提出修改，按意见修复。
- 真实文件上传、课件附件预览/下载仍待后续轮次，由组长裁决是否进入下一轮。
