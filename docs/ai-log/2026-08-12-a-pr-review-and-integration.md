# 2026-08-12 A 成员：PR 顺序审查、合并与集成整改

## 日期

2026-08-12。

## 目标

依次处理待合并 PR。发现问题时先写入 PR，会合并后在 `develop` 修复。补充按 B、E 原始任务单复核交付范围。

## 完整提示词

> 依次处理几个PR，如果存在问题，在PR中提出，在develop中修复

补充提示：

> 这是BE的任务，补充给你查看

附带文件为 `02-B-任务.md` 和 `05-E-任务.md`，分别要求 B 接入家长端真实认证和公共类型，E 接入后台认证、校区权限并承担 Windows 验收。

## AI 回复摘要

- 确定顺序为 PR #7 → PR #6 → PR #5。
- PR #7 无阻塞问题，先合并公共依赖。
- 在 PR #6 记录退出异常不清 Token、Mock 时间格式和教师 ID/名称矛盾。
- 在 PR #5 记录未登录页面入口、审计用户 ID、重复角色类型和缺少独立交付记录。
- 三个 PR 合并后，只在 `develop` 完成整改和测试。

## 人工判断

- [x] 公共依赖先于成员认证接入合并，顺序正确。
- [x] 成员分支保留原始交付，问题均在 PR 会话可追溯。
- [x] 修复没有扩展公共字段，也没有改动 C、D 模块。
- [x] B、E 原始任务中的功能与测试已逐项复核。

## 执行的修改

- 家长端退出使用 `finally` 清除本地 Token，并补 5xx、断网测试。
- 家长端 Mock 时间统一公共格式，页面仍以 `HH:mm` 展示。
- 家长端教师 ID 与公共账号展示名统一，并补关联测试。
- 后台仅向已登录管理员展示业务导航，页面切换增加认证守卫。
- 后台审批人和工单处理人使用认证用户真实 ID。
- 后台 `AdminRole` 由公共 `UserRole` 派生，不再重复维护角色值。
- 新增 E 独立交付记录，更新第一周计划、页面清单和 A 认证交付文档。

## 测试结果

- macOS `npm ci`：通过。
- `npm run check`：通过。
- 全仓共发现 72 项测试：71 项通过、0 失败、1 项真实 HTTP 端口测试因当前沙箱禁止监听而跳过。
- 家长端：18/18 通过。
- 后台：17/17 通过。
- 六个工作区类型检查和构建通过。
- 浏览器实测后台未登录/登录/退出导航边界和家长端登录/课表/通知/退出流程通过；两端控制台 0 错误、0 警告。
- `git diff --check`：通过。
- E 已提供 Windows `npm ci`、`npm run check`、API 启动和认证流程通过记录。

## 下一轮问题

1. C 仍需补学生端在 Windows 的独立启动记录。
2. B、D、E 后续按公共 ID 联调调课通知、审批和代课数据。
