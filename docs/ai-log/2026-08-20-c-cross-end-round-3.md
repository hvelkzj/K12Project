# AI 协作记录：C 学生端第三轮跨端联调

日期：2026-08-20

## 目标

按第三轮任务 `03-C-任务(2).md` 完成跨端联调验证：真实 API 走通发布→提交→批改→订正→查看结果，验证截止/附件/重复提交/401/422/网络错误边界与账号停用会话失效，修复学生端状态、完成率、草稿和历史记录问题；不修改 API、公共类型或其他前端。

## 完整提示词

```text
"D:\Wechat\...\03-C-任务(2).md" "D:\Wechat\...\00-本轮说明(1).md" 继续完成下一轮任务
```

## AI 回复摘要

- 确认第一、二轮 PR（#17、#18）已合并，A 第三轮（#22）已合并，develop 更新到 `3aab12c`；基于最新 develop 创建 `fix/C-student-cross-end-round-3`。
- 阅读 A 的跨端集成测试与 businessStore 契约，确认发布/批改/停用接口形态。
- 编写并运行 21 项真实 API 联调检查（教师发布、学生读取/提交、教师端读取、要求订正、查看分数评语、订正 attempt 2、完整历史、批改 GRADED、重复提交 409、限时过期 409、空内容 422、无效 Token 401、账号停用会话失效与恢复），全部通过。
- 修复学生端两个问题：提交结果页立即展示本次提交；attempt 历史展示正文/分数/评语。
- 全仓 `npm run check` 通过后整理交付记录。

## 人工判断

- 截止后 409 用真实 API 验证（发布 2 秒后截止的作业并等待过期）；API 禁止直接发布已过期作业属正常保护，不需要修改。
- 网络错误无法在联调中停服验证，由学生端单元测试覆盖 `NETWORK_ERROR`。
- 学生端改动仅限 `views/Home.vue`（结果页与历史展示），未触碰业务接口与草稿存储逻辑。

## 执行的修改

- 修改 `apps/student-web/src/views/Home.vue`：新增 `lastSubmitted` 立即回显、`displayedSubmission` 计算属性、完整 attempt 历史渲染与样式。
- 新增 C 第三轮交付记录和本 AI 记录。

## 测试结果

- 真实 API 联调 21 项全部通过。
- 学生端测试：58/58 通过。
- 学生端 typecheck / build：通过。
- 根目录 `npm run check`：通过。

## 下一轮问题

- 等待本 PR 评审；账号停用、限时截止等跨端场景可纳入后续回归。
- 若需更完整的"完整 attempt 历史"（如附件列表），由 A 裁决是否扩展概览响应。
