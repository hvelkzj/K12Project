# 2026-07-27 A 成员：初期项目架构

## 目标

完成 A 成员 7/27 的任务：建立后端和四个前端项目骨架，配置启动、检查和环境变量示例，并完善交接文档。

## 完整提示词

> 我是本项目中的A成员，请完成7-27的任务，帮我完成初期的项目架构，并且完善文档内容。

补充上下文：用户提供了 `docs/week-1-execution-plan.md` 的 Typora 截图，并要求按项目中的 A 成员分工执行。

## AI 回复摘要

- 建立 npm 工作区。
- 建立 Node.js + TypeScript 后端。
- 建立四个 Vue 3 + TypeScript 前端。
- 增加统一启动、代码规范、类型检查、测试和构建命令。
- 增加环境变量示例、健康检查和基础测试。
- 更新 README 和第一周执行计划。

## 人工判断

待 A 成员审查后填写：

- [ ] 目录分工符合小组约定。
- [ ] 端口和启动命令可以交给 B/C/D/E。
- [ ] 后续数据库和公共字段仍由 A 统一维护。

## 执行的修改

- 新增 `apps/api`。
- 新增 `apps/parent-web`、`apps/student-web`、`apps/teacher-web`、`apps/admin-web`。
- 新增根目录工作区、统一脚本、TypeScript 和 ESLint 配置。
- 新增每个应用的 `.env.example`。
- 更新 `README.md` 和 `docs/week-1-execution-plan.md`。

## 测试结果

- `npm run check`：通过。
- ESLint：通过。
- 五个项目类型检查：通过。
- 后端基础测试：2 个通过。
- 后端和四个前端生产构建：通过。
- 开发模式启动验证：3000、5173、5174、5175、5176 均返回 HTTP 200。

## 下一轮问题

1. B 的家长、学生绑定、请假和通知字段是什么？
2. C 的作业、附件、提交和批改结果字段是什么？
3. D 的签到、反馈、调课和批改字段是什么？
4. E 的审批、代课、校区权限和测试数据字段是什么？
