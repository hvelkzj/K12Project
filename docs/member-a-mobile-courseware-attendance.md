# A 多端、课件与考勤完善交付记录

日期：2026-08-25

分支：`feature/A-student-mobile`

## 交付内容

- 新增 `apps/student-mobile`，使用 uni-app、Vue 3 和 TypeScript 维护一套学生移动端源码。
- 完成登录、首页、课件、作业、作业详情/提交和我的 6 个中文业务页面。
- APP 和小程序接入真实登录、当前用户、退出、学生概览、文件上传下载和作业提交接口。
- 移动端支持拍照/选择 JPG、PNG；微信小程序支持从会话选择 PDF、DOCX、JPG、PNG；单文件限制 10 MB。
- 公共包新增 `StudentOverview`，学生网页、APP、小程序和统一服务共用相同契约。
- 教师端新增课件发布，统一服务校验授课关系、附件归属和同名重复发布。
- 学生概览与家长学生概览返回考勤；学生网页、移动端和家长课表显示中文考勤状态。
- 新增 base64 文件传输方式，移动端上传后服务端按原始字节执行格式和大小校验。
- 新增六角色压力测试脚本，验证概览吞吐和跨角色访问隔离。

## 业务联动

| 流程 | 结果 |
|---|---|
| 教师发布课件 → 学生网页端读取 | 通过 |
| 教师发布课件 → APP/小程序通过学生概览读取 | 通过公共客户端、服务测试和双端构建验证 |
| 移动端上传文件 → 创建提交 | 上传成功后才创建提交，失败保留正文 |
| 教师批改 → 移动端显示分数、评语和历史 attempt | 完成 |
| 教师保存考勤 → 学生和家长读取 | 通过，后端按本人/绑定关系过滤 |

## 权限和错误处理

- 移动端仅允许 `STUDENT`，其他角色登录后立即撤销会话。
- 401 清理本地会话并返回登录页；403 显示无权进入；409、422 显示服务端中文原因。
- 网络错误保留已加载数据、作业正文和待提交附件，提交期间按钮禁用。
- 教师只能为本人授课班级和课程发布课件，不能引用其他账号上传的附件。
- 学生只读取本人考勤，家长只读取绑定学生考勤。

## 构建产物

| 产物 | 状态 |
|---|---|
| `apps/student-mobile/dist/build/app/` | uni-app APP 运行资源已生成 |
| `apps/student-mobile/dist/build/mp-weixin/` | 微信小程序构建目录已生成，可导入开发者工具 |
| Android 测试 APK | 需要在安装 HBuilderX/Android 运行环境的设备上生成并安装复验 |

构建目录受根 `.gitignore` 管理，不进入源码提交；最终交付时按课程要求单独保存证据。

## 测试结果

- `npm run check`：通过。
- 全仓：321 项有效测试通过，0 失败；API 2 项真实 HTTP 用例在受限测试进程中跳过。
- 移动端：22 项通过，覆盖登录、角色拒绝、会话、概览、课件筛选、作业状态、附件规则、考勤中文状态和常见错误。
- API：58 项有效测试通过，包含课件发布、考勤范围和移动端 base64 附件。
- 教师端：53 项通过；家长端 39 项、学生网页端 69 项通过。
- `npm run build:app`：通过。
- `npm run build:mp-weixin`：通过。
- `npm run test:load`：500 请求、并发 50、失败 0；详细指标见 `docs/load-test-2026-08-25.md`。

## 公共文件影响

- `packages/shared`：新增唯一公共 `StudentOverview`，未新增重复角色或状态。
- `apps/api`：扩展概览、课件发布和移动文件传输。
- 根 `package.json`、`package-lock.json`：登记移动工作区依赖和跨平台 npm 脚本。
- 公共契约、业务规则、项目状态、页面清单和报告证据已同步更新。

## 后续验收

- Windows PowerShell 执行 `npm ci`、`npm run check`、`npm run test:load`、`npm run build:app` 和 `npm run build:mp-weixin`。
- HBuilderX/Android 模拟器或真机生成并安装测试 APK。
- 微信开发者工具导入小程序构建目录，使用 `student_101` 完成完整业务流程并保存真实截图。
