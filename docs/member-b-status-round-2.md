# B 家长端第二轮状态补齐交付记录

完成日期：2026-08-22

分支：`feature/B-parent-status-round-2`

## 已完成

- 接入 `PATCH /parent/notifications/:notificationId/read`。
- 通知页点击“查看并标记已读”后调用真实 API。
- 已读接口成功后才更新普通通知或调课通知的 `readAt`。
- 已读接口失败时保持未读状态并显示服务端或网络错误。
- 首页显示未读通知数量。
- 请假页展示 `PENDING`、`APPROVED`、`REJECTED` 三种状态的家长可读文案。
- 请假记录展示审核意见和更新时间。
- 请假提交成功后刷新当前学生概览，同步最新审批状态。
- 请假页支持手动刷新审批状态，并保持当前学生选择。
- 防止通知已读和请假提交重复点击。
- 业务接口 `401` 清除 Token 并返回登录页；其他业务错误保留会话。
- 页面运行时未读取业务 Mock。

## 未修改范围

- 未修改 API。
- 未修改公共类型。
- 未修改其他前端。

## 测试覆盖

- 通知已读成功。
- 通知已读 `409` 和网络错误。
- 未读通知数量回写。
- 普通通知和调课通知已读状态替换。
- 请假三种状态展示。
- 请假审核意见和更新时间页面展示逻辑。
- 请假重复提交、字段错误和网络错误。
- `401` 清除 Token，其他业务错误保留登录状态。

## 需要配合

- 成员 A/API 侧保持通知已读接口路径、请求体和返回 `Notification` 稳定。
- 请假批准和拒绝由后台完成，家长端通过刷新当前学生概览展示最新状态。

## PR #27 评审修复

- 真实 API 会把调课通知同时放入 `notifications` 和 `scheduleChangeNotices`；页面现在按通知 ID 去重并优先保留完整调课字段。
- 新增通知 `403`、`404`、`409` 和网络错误测试。
- 新增旧概览响应和通知重复读取保护测试。
- 修复后家长端 35/35 项测试通过。
