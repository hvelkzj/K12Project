# A 第四轮交付记录：最终全仓集成

完成日期：2026-08-23

分支：`fix/A-final-integration-round-4`

## 完成范围

- 基于 B、C、D、E 第四轮 PR 合并后的最新 `develop` 执行最终检查。
- 复核共享类型、六角色认证、业务 API、管理 API、内存数据仓库和根启动脚本。
- 确认作业、调课、反馈、请假、排课和账号七条跨端流程通过。
- 增加真实 HTTP 六角色认证回归，每个角色都验证登录、当前用户、退出和旧令牌失效。
- 增加根启动地址和跨平台 npm 脚本的回归测试。
- 更新 README、项目完成状态、模块清单、数据说明和后续任务归档。

## 最终集成修复

首次根目录启动时，API 监听 `127.0.0.1`，但四个 Vite 前端默认只在 `localhost`/IPv6 上可用，文档中的 `127.0.0.1:5173–5176` 无法访问。

已在根目录修复：

- `npm run dev` 为四个前端统一传入 `--host 127.0.0.1`。
- `npm run dev:parent`、`dev:student`、`dev:teacher`、`dev:admin` 使用相同地址。
- 脚本仍使用 Node.js 和 npm 参数，未增加 macOS 或 Windows 专用命令。

## 跨端验证

| 流程 | 结果 |
|---|---|
| 教师发布 → 学生提交 → 教师批改 → 学生查看 | 通过 |
| 教师要求订正 → 学生再次提交 → 保留 attempt 历史 | 通过 |
| 教师调课 → 教务审批/代课 → 家长通知已读 | 通过 |
| 教师反馈 → 家长异议 → 后台关闭工单 | 通过 |
| 家长请假 → 后台审批 → 教师只能登记 `LEAVE` | 通过 |
| 后台新增/修改排课 → 教师概览同步 | 通过 |
| 后台停用账号 → 撤销会话 → 禁止再登录 | 通过 |

## macOS 验证

```text
npm ci
npm run check
npm run dev
```

- `npm ci`：通过。
- `npm run check`：通过，全仓 270 项测试通过、0 失败、0 跳过。
- API 测试：51/51 通过，包含真实 HTTP 六角色会话回归。
- 根目录启动：API、家长端、学生端、教师端和后台均启动。
- `127.0.0.1:3000`、`5173`、`5174`、`5175`、`5176` 均返回 HTTP 200。
- 六角色实际登录和会话恢复返回 200，退出返回 204，退出后旧令牌返回 401。

## Windows PowerShell 验证

复核日期：2026-08-24。环境：Windows 11 专业版、PowerShell 7.6.4、Node.js 24.17.0、npm 11.8.0。

```text
npm ci
npm run check
npm run dev
npm audit
```

- `npm ci`：通过，按根锁文件完成干净安装。
- `npm run check`：通过，全仓 300 项测试通过、0 失败、0 跳过，lint、类型检查和全部工作区构建通过。
- 工作区测试：shared 14、API 68、统一入口 7、家长端 39、学生端 67、教师端 48、后台 57。
- 根目录启动：API、统一入口和四个业务前端均启动，`3000`、`5172`、`5173`、`5174`、`5175`、`5176` 均返回 HTTP 200。
- 十三个账号实际登录返回 200 且角色匹配，当前用户返回 200，退出返回 204，退出后旧令牌返回 401；学生注册、登录和概览返回 201、200、200。
- PowerShell 停止根启动后，六个端口均已释放。
- `npm audit` 和 `npm audit --omit=dev`：均为 0 漏洞。
- Node.js 24 发现的 `DEP0190` 启动警告已修复，统一启动不再启用 shell。
- 完整过程见 [Windows 项目测试记录](windows-test-2026-08-24.md)。

## 公共影响

- `scripts/dev.mjs` 改为通过当前 Node.js 进程调用 npm CLI，保留跨平台启动。
- `packages/shared/test/frontendIntegration.test.ts` 增加无 shell 启动回归测试。
- `package-lock.json` 仅更新 4 个受安全公告影响的间接依赖补丁版本。
- 未新增业务接口、公共字段、角色、状态或直接依赖。
- 未修改统一入口和 B、C、D、E 页面代码。
